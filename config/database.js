module.exports = {
  'url': process.env.MONGODB_URI // mongodb settings
};

// mongoose.Promise = global.Promise;
// mongoose.connect(process.env.MONGODB_URI || 'mongodb://localhost:27017/todolist',{
//   useMongoClient: true,
// }); 